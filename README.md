# H.D. Transformers — Public Website
Deploy this folder as a standalone Vercel project. The entry point is `index.html`.
The catalogue contains 54 prepared products and local SVG product artwork so product cards never show broken-image icons.
Firebase configuration is in `app.js`. Customer data writes are protected by Firestore rules.
