import { db, functions } from "./shared/firebase.js";import { collection, query, where, getDocs, doc, getDoc } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js";import { httpsCallable } from "https://www.gstatic.com/firebasejs/12.16.0/firebase-functions.js";const DEFAULT_IMG="assets/default-transformer.svg",submit=httpsCallable(functions,"submitInquiry"),slug=new URLSearchParams(location.search).get("slug")||"";const host=document.getElementById("productDetail"),related=document.getElementById("relatedGrid");const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[m]));const money=v=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(Number(v)||0);let settings={whatsapp:"917949347417",phone:"07949347417"};const ss=await getDoc(doc(db,"site","public"));if(ss.exists())settings={...settings,...ss.data()};const ps=await getDocs(query(collection(db,"products"),where("slug","==",slug),where("active","==",true)));const p=ps.docs.map(d=>({id:d.id,...d.data()})).find(x=>x.active===true);if(!p){host.innerHTML='<div class="not-found"><h1>Product not found</h1><a href="index.html#products">View catalogue</a></div>';throw new Error("Product not found")};document.title=p.seoTitle||`${p.name} Manufacturer in Jaipur | H.D. Transformers`;document.querySelector('meta[name="description"]').content=p.seoDescription||p.description||"H.D. Transformers product";const gallery=[...new Set([p.imageUrl,...(p.gallery||[])].filter(Boolean))];host.innerHTML=`<section class="product-detail"><div class="detail-gallery"><div class="detail-main-image"><img id="detailMainImage" src="${esc(gallery[0]||DEFAULT_IMG)}" alt="${esc(p.name)}"></div><div class="thumbs">${gallery.map((u,i)=>`<button data-img="${esc(u)}"><img src="${esc(u)}" alt="${esc(p.name)} image ${i+1}"></button>`).join("")}</div></div><div class="detail-copy"><span class="detail-category">${esc(p.category||"Transformer")}</span><h1>${esc(p.name)}</h1><div class="detail-price">${p.showPrice&&+p.price?money(p.price)+" / "+esc(p.priceUnit||"Piece"):esc(p.priceLabel||"Get Latest Price")}</div><div class="detail-status">${esc((p.availability||"made_to_order").replaceAll("_"," "))} • MOQ ${Number(p.moq||1)}</div><p>${esc(p.description||"")}</p><div class="detail-actions"><button class="btn primary" data-action="inquiry">Get Best Quote</button>${p.callbackEnabled!==false?'<button class="btn outline" data-action="callback">Request Callback</button>':''}${p.orderEnabled!==false&&p.availability!=="unavailable"?'<button class="btn dark" data-action="order">Order Request</button>':''}<a class="btn whatsapp" id="waProduct" target="_blank" rel="noopener">WhatsApp</a>${p.brochureEnabled!==false&&p.brochureUrl?`<a class="btn outline" href="${esc(p.brochureUrl)}" target="_blank" rel="noopener">Product Brochure</a>`:""}</div><div class="quick-trust"><span>✓ Direct company enquiry</span><span>✓ Jaipur, Rajasthan</span><span>✓ Technical requirement support</span></div></div></section><section class="spec-section"><div><span class="kicker">Technical details</span><h2>Product specifications</h2></div><div class="spec-table">${(p.specs||[]).map(s=>`<div><span>${esc(s.label)}</span><strong>${esc(s.value)}</strong></div>`).join("")||'<div><span>Specifications</span><strong>Contact sales for verified configuration</strong></div>'}</div>${p.videoUrl?`<p><a class="text-link" href="${esc(p.videoUrl)}" target="_blank" rel="noopener">Watch product video ↗</a></p>`:""}</section><section class="inline-lead"><div><span class="kicker">Buying requirement</span><h2>Tell us what you need.</h2><p>Product is pre-selected automatically.</p></div><form id="detailLead"><div class="row"><label>Name<input id="dName" required maxlength="80"></label><label>Phone<input id="dPhone" required maxlength="20"></label></div><div class="row"><label>Email<input id="dEmail" type="email"></label><label>City<input id="dCity"></label></div><div class="row"><label>Quantity<input id="dQty" type="number" min="1" value="1"></label><label>Request type<select id="dType"><option value="inquiry">Enquiry / Quote</option><option value="order">Order Request</option><option value="callback">Request Callback</option></select></label></div><label>Custom message<textarea id="dMessage" rows="5" placeholder="Rating, phase, application, delivery location..."></textarea></label><div class="detail-actions"><button class="btn primary" type="submit">Send to Owner Dashboard</button><button id="dWhatsApp" class="btn whatsapp" type="button">Send on WhatsApp</button></div><p id="dStatus"></p></form></section>`;host.querySelectorAll(".thumbs button").forEach(b=>b.onclick=()=>document.getElementById("detailMainImage").src=b.dataset.img);document.getElementById("detailMainImage").onerror=e=>e.currentTarget.src=DEFAULT_IMG;host.querySelectorAll("[data-action]").forEach(b=>b.onclick=()=>{document.getElementById("dType").value=b.dataset.action;document.getElementById("detailLead").scrollIntoView({behavior:"smooth"})});document.getElementById("waProduct").href=`https://wa.me/${String(settings.whatsapp).replace(/\D/g,"")}?text=${encodeURIComponent(`Hello H.D. Transformers, I am interested in ${p.name}.`)}`;function payload(channel){return{type:document.getElementById("dType").value,channel,productId:p.id,productName:p.name,customerName:document.getElementById("dName").value.trim(),phone:document.getElementById("dPhone").value.trim(),email:document.getElementById("dEmail").value.trim(),city:document.getElementById("dCity").value.trim(),quantity:Math.max(1,+document.getElementById("dQty").value||1),message:document.getElementById("dMessage").value.trim()}}document.getElementById("detailLead").onsubmit=async e=>{e.preventDefault();const st=document.getElementById("dStatus");st.textContent="Sending…";try{const r=await submit(payload("dashboard"));st.textContent=`Sent successfully. Reference: ${r.data.reference}`}catch(err){console.error(err);st.textContent="Could not send. Please call or WhatsApp."}};document.getElementById("dWhatsApp").onclick=async()=>{const d=payload("whatsapp");if(d.customerName.length<2||d.phone.replace(/\D/g,"").length<6){document.getElementById("dStatus").textContent="Enter name and phone first.";return}try{await submit(d)}catch{}const msg=["Hello H.D. Transformers,",`${d.type}: ${p.name}`,`Quantity: ${d.quantity}`,`Name: ${d.customerName}`,`Phone: ${d.phone}`,d.message?`Message: ${d.message}`:""].filter(Boolean).join("\n");window.open(`https://wa.me/${String(settings.whatsapp).replace(/\D/g,"")}?text=${encodeURIComponent(msg)}`,"_blank","noopener,noreferrer")};const rel=await getDocs(query(collection(db,"products"),where("active","==",true)));rel.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.id!==p.id&&(x.categorySlug===p.categorySlug||x.category===p.category)).slice(0,3).forEach(x=>{const a=document.createElement("a");a.className="related-card";a.href=`product.html?slug=${encodeURIComponent(x.slug||x.id)}`;a.innerHTML=`<img src="${esc(x.imageUrl||DEFAULT_IMG)}"><div><small>${esc(x.category)}</small><strong>${esc(x.name)}</strong><span>${x.showPrice&&+x.price?money(x.price):esc(x.priceLabel||"Get Latest Price")}</span></div>`;related.appendChild(a)});
